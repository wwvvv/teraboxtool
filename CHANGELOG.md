# TeraBox 链接替换工具 - 版本更新记录

## v2.1.0 (2026-05-05)
### 🐛 修复
- **认证修复**: `getShareInfo()` 移除 jsToken/bdstoken/Cookie 认证参数（这些参数反而导致公开接口返回 `errno=4000020`）；Cookie 变量兼容 `TERABOX_COOKIE` 和 `TERABOX_NDUS` 两种命名
- **状态修复**: `errno=4000020` 错误从 `DELETED`（已删除）改为 `INVALID`（资源失效），语义更准确
- **文件名校验**: `transfer.js` 增加兜底逻辑，当 API 失败但 HTML/记录中已有文件名时仍可做文件名查重

### 🚀 新特性
- **Docker 支持**: 新增 `Dockerfile`、`docker-compose.yml`、`.dockerignore`，支持容器化部署
- **全流程补全**: `run` 命令现在包含 `sync_filename` 步骤，完整流程为 `crawl → sync_filename → transfer → share → replace`

### 🛠️ 优化
- **域名兼容**: `listDir` 增加 `terabox.app` 域名尝试；`getShareInfo` 各域名错误响应详细日志
- **跨域兼容**: `transfer` 函数改为多域名轮询，失败时自动切换域名
### 🛠️ 优化与修复
- **持久化日志**: 更新了 `logger.js`，现在所有运行日志都会自动保存到 `data/logs/app.log` 文件中，方便后续排查问题和留档。

## v2.0.2 (2026-05-04)

### 🚀 重大重构 (Major Refactor)
- **采集逻辑升级**: 查重机制改为按文章ID (`postId`)，记录中已有同ID文章则整篇跳过。新增记录 `downloadUrl`（采集时的原地址）。
- **转存逻辑升级**: 拆分转存与分享。转存前先拉取网盘目录，按**文件名精确查重**，目录中已有同名文件则跳过转存，直接复用。
- **分享逻辑升级**: 彻底重构分享流程。按文件名精确匹配网盘文件，创建分享后**自动回填到所有相同文件名的记录**，实现“一次分享，全表同步”。
- **替换逻辑升级**: 增加**新旧地址比对**。替换前比对当前线上地址与新分享地址，若已是新地址或新旧相同则自动跳过，仅替换旧地址。
- **状态逻辑重建**: 规范化状态流转：`已采集` -> `已转存` -> `已分享` -> `已替换`。新增“重置状态”功能，支持回退到指定阶段。
- **WebUI 深度改版**:
  - 表格新增“文件名”和“下载地址”列，支持按文件名检索。
  - 新增“初始化/重置”功能弹窗，支持选择重置到采集或转存阶段。
  - 增强批量操作：支持对选中项进行“转存”、“分享”、“替换”和“重置”。
  - 界面优化，提升大数据量下的响应速度。

### 🛠️ 优化与修复
- **Records 模块**: 自动迁移旧版 `fileNames[]` 数组为单字符串 `fileName`。
- **API 性能**: 减少冗余的 TeraBox API 调用，通过本地缓存和文件名查重提升配额利用率。

## v1.5.0 (2026-05-04)

### ✨ 新特性
- **文件名匹配查重**: 新增 `fileNames` 字段，记录原链接内的文件名称（不含后缀）。
  - **转存查重**: 在转存前检查是否有相同文件名的记录已处理，若有则直接复用，极大节省转存配额。
  - **分享查重**: 在创建分享链接前检查是否已有同文件名的分享链接，防止重复创建。
- **数据库同步脚本**: 新增 `sync_db.js` 脚本，支持批量补全历史记录中的文件名信息。
- **管理面板升级**: 
  - 列表新增“文件名”展示列，并调整至“标题”列后方便对比。
  - 搜索功能支持按文件名进行关键词检索。
  - CSV 导出增加“文件名”列。

### 🛠️ 优化
- **Records 模块**: 升级数据模型，支持 `fileNames` 列表的持久化存储。
- **UI 版本**: 更新至 v1.5.0。

## v1.4.1 (2026-05-03)

### 🐛 修复
- **分享链接生成**: 修复 `createShare` 调用 `share/pset` 始终返回 `errno=2` 的问题。
  - 域名从 `terabox.com` / `1024terabox.com` 切换为 `terabox.app`
  - 移除 URL 中不必要的 `jsToken` / `bdstoken` 参数
  - 新增 `path_list` 参数（文件完整路径）
  - `channel_list` 从 `[]` 修正为 `[0]`
- **分享调用**: `share.js` 传入文件路径参数，提升匹配成功率。

## v1.4.0 (2026-05-03)

### ✨ 新特性
- **数据初始化**: 管理面板新增"数据初始化"按钮，支持批量重置记录状态。

## v1.3.0 (2026-05-03)

### ✨ 新特性
- **强行终止**: 在任务执行过程中，新增“🛑 停止任务”按钮，可随时中断正在进行的采集、转存、分享或替换任务。

### 🛠️ 优化
- **任务调度**: 引入全局运行状态管理，提升了对长时间运行任务的控制能力。

## v1.2.0 (2026-05-03)

### ✨ 新特性
- **手动编辑**: 记录表新增“编辑”功能，支持通过弹窗手动修改原链接、新链接、提取码及任务状态。

### 🗑️ 移除
- **自动检测**: 移除了不稳定的自动失效链接检测功能，改为通过手动编辑或批量删除来维护记录。

### 🛠️ 优化
- **状态管理**: 优化了记录状态的流转逻辑，支持手动干预任务进度。
- **后端 API**: 新增单条记录更新接口，提升数据维护灵活性。

## v1.1.0 (2026-05-03)

### ✨ 新特性
- **采集查重**: 采集时会自动比对记录表中的原链接和新链接，防止重复采集已处理或已存在的资源。
- **失效检测**: 增加 `check` 模块，可批量检测记录表中的链接是否失效，并在界面上标记为“链接失效”。
- **单选/多选处理**: 管理面板支持勾选单条或多条记录，进行针对性的转存、分享、检测、替换或删除操作。

### 🛠️ 优化
- 改进 `Records` 模块，支持更精准的查重逻辑。
- 优化 `server` 接口，支持接收目标列表进行局部任务执行。
- UI 增加选中状态样式和批量操作浮动栏。

## v1.0.0 (2026-05-03)

### 🎉 首次发布

#### 核心功能
- **采集模块** (`crawl`): 通过 WordPress REST API 获取作者ID:5的所有文章，从 `xun_post_download` 字段提取 TeraBox 下载链接
- **转存模块** (`transfer`): 通过 TeraBox API 批量转存原网盘资源到自己的 `/acgx/` 目录
- **分享模块** (`share`): 通过 TeraBox API 为已转存文件批量创建公开分享链接
- **替换模块** (`replace`): 通过 WordPress REST API 将新分享链接回写到文章的 `xun_post_download` 字段
- **全流程** (`run`): 一键串行执行 crawl → transfer → share → replace

#### 记录表管理
- JSON 格式记录表，以 `postId + originalLink` 为唯一键自动查重
- 6 种状态追踪：待处理、已采集、已转存、已分享、已替换、失败
- 支持 CSV 导出
- 支持从旧版 `all_links_table.json` 数据导入

#### Web 管理面板
- Vue 3 SPA 暗色主题管理界面
- 实时状态统计卡片
- 记录表搜索、状态筛选、分页
- 一键任务触发
- 实时运行日志面板

#### 技术栈
- Node.js + Express 后端
- Vue 3 CDN 前端
- TeraBox API (纯 HTTP, 无 Puppeteer)
- WordPress REST API + xun 主题 AJAX 接口
- 统一日志系统 (终端彩色 + Web 推送)
