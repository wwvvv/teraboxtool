services:
  teratool:
    build: .
    container_name: teratool
    ports:
      - "127.0.0.1:3721:3721"
    environment:
      - PORT=3721
    volumes:
      - ./data:/app/data
    restart: unless-stopped